# Windows_Security_Monitoring_System
The Windows Security Monitoring System is a Python-based security tool that monitors Windows Event Logs in real time to detect unauthorized login attempts. When multiple failed login attempts are detected, the system automatically triggers security actions such as capturing an image, sending email alerts, and locking the system.
